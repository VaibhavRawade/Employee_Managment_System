# Import Environment Variables from config.properties file

# Spring Boot Application Configuration
spring.application.name=Employee-Management

spring.datasource.url=jdbc:postgresql://localhost:5432/React_DB
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver


file.upload-dir=C:/Users/shree/Desktop/React-Project-29May2026/images

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# MongoDB Configuration (optional — not currently used by any repository)
#spring.data.mongodb.uri=${MONGO_URI:}
#spring.data.mongodb.auto-index-creation=false

# Server Configuration
server.port=8082

# JWT Configuration
jwt.secret=mysecretkey

#twilio.phone.number=+919604951218


twilio.account.sid=${TWILIO_ACCOUNT_SID}
twilio.auth.token=${TWILIO_AUTH_TOKEN}
twilio.verify.sid=${TWILIO_VERIFY_SID}

razorpay.key.id=${RAZORPAY_KEY_ID}
razorpay.key.secret=${RAZORPAY_KEY_SECRET}
